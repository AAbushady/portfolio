<span class="pill"><slot /></span>

<style>
	.pill {
		display: inline-block;
		font-family: var(--font-mono);
		font-size: 0.75rem;
		line-height: 1.6;
		color: var(--gray-400);
		text-transform: lowercase;
		white-space: nowrap;
	}

	.pill::before {
		content: '[';
	}

	.pill::after {
		content: ']';
	}
</style>
